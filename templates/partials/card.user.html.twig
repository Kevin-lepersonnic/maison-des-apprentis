<div class="col-xl-4 col-lg-4 col-md-8 col-sm-12 m-auto text-center">
    <div class="card bg-light my-5 shadow ">
        <h3 class="card-header">{{user.fullname | title}}</h3>
        <img height="350px" class="rounded-circle border border-success m-2" src="{{user.avatar}}" alt="Card image">
        <div class="card-body">
          {# <p class="card-text">{{user.presentation}}</p> #}
          <a href="{{ path('user_show', {'slug':user.slug})}}" class="btn btn-primary rounded">Lire la suite</a>
          <div class="my-3">
              <a href="#" class="card-link">Amar</a>
          </div>
        </div>
        <div class="card-footer text-muted">
        </div>
    </div>
</div>

